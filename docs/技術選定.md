# 技術選定

ステータス: 進行中

# TRD（技術要件定義書）v3 — 秘密オークション／Supabase完結型スタック確定版

作成日：2026年8月7日
対象：PWA一本（iOS Web Pushの制約は受け入れる）
開発体制：小規模チーム（2〜5人・学生レベル）
本バージョンでSupabase完結型に一本化する（2026-08-17: Frontend/データアクセス層はNext.js + Prismaに変更。Auth/RLS/Realtime/Storage/PostgreSQL FunctionsはSupabaseのまま。詳細は Frontend章と `docs/アーキテクチャ比較.md`）。

---

### Frontend

**2026-08-17変更**: Vite + TanStack Router から **Next.js（App Router）** に変更（確定）。Prisma導入とあわせ、Vite + Supabase直結案との比較検討を経てリーダー判断で維持を決定。理由・比較は [`docs/アーキテクチャ比較.md`](./アーキテクチャ比較.md)、実装構成は [`docs/アーキテクチャ.md`](./アーキテクチャ.md) を参照。

```
├─ React 19
├─ TypeScript
├─ Next.js（App Router）
├─ Prisma（型付きDBアクセス。実行時のRLSは lib/db/rls.ts で再現。詳細は docs/アーキテクチャ.md）
├─ Zod
├─ Tailwind CSS
├─ shadcn/ui
├─ PWA（Progressive Web App）
├─ Service Worker（最後に時間が余ったら）
├─ Web Push API（最後に時間が余ったら）
├─ Vitest
├─ Testing Library
└─ Playwright（最後の仕上げ前に必要なら）
```

TanStack Query / Zustand / Oxlint / Oxfmt は Next.js への変更に伴い未採用（Server Actions / Server Components 中心の設計のため、クライアント状態管理・データフェッチライブラリの必要性が下がった。必要になった時点で追加検討）。

### Backend

```
├─ Supabase
│  ├─ PostgreSQL（DB本体。接続先はSupabase）
│  ├─ Auth
│  ├─ Realtime
│  ├─ Storage
│  └─ Edge Functions
├─ Prisma（スキーマ定義 / マイグレーション / 型生成。DBはSupabaseのPostgresを指す）
├─ PostgreSQL Functions（PL/pgSQL）← ビジネスロジックの中心
├─ RLS（Row Level Security）
├─ seed.sql（Prisma seed / SQLどちらかに統一。詳細は5章）
├─ Deno lint / fmt / check / test
└─ pgTAP（重要な処理のみ）
```

マイグレーションは Supabase CLI ではなく **Prisma Migrate** で管理する（2026-08-17変更）。PostgreSQL Functions・RLSポリシーなどのSupabase固有SQLはPrismaのスキーマ言語では表現できないため、Prismaマイグレーションに生SQLとして含めるか、別途SQLファイルで管理するかは5章の未確定事項とする。

**Auth方式（2026-08-18実装時に確定、2026-08-22改訂）**: 当初はMagic Link（パスワードレス、`signInWithOtp`）のみを実装したが、UIには最初からGoogleログイン・メール+パスワードの導線があり、Magic Link用のUIは作られなかった。2026-08-22、UIに存在しないMagic Linkのバックエンド実装（`signInWithMagicLink`）を削除し、実際にUIから使われているGoogle OAuth（`signInWithOAuth`、issue #72）とメールアドレス・パスワード（`signInWithPassword`/`signUpWithPassword`、issue #76）の2方式に整理した。実装は`features/auth/`（`prisma/sql/auth/001_create_profile.sql` / `docs/DB.md` §6.1）。

### DevOps

```
├─ Prisma CLI（マイグレーション・型生成）
├─ Supabase CLI（Auth / RLS / Storage / Edge Functions等の運用）
├─ Docker
├─ GitHub Actions
└─ Cloudflare Pages
```

---

## 1. ビジネスロジックの配置方針（確定）

**原則：複数テーブルにまたがる整合性が必要な処理はPostgreSQL Functionに、外部I/Oが絡む副作用処理はEdge Functionsに置く。**

| 処理 | 実装場所 | 理由 |
| --- | --- | --- |
| 入札処理（価格検証・行ロック・Wallet残高チェック・更新） | PostgreSQL Function | 同一トランザクション内で完結させ、レースコンディションを防ぐ |
| ポイントのcredit/debit | PostgreSQL Function | グループ完全分離の不変条件をSQLレベルで強制 |
| オークション終了確定処理 | PostgreSQL Function（`pg_cron`から定期実行） | 複数テーブルの一括更新をトランザクションで保証 |
| 複数人承認判定・ポイント付与 | PostgreSQL Function | 承認集計とポイント付与を同一トランザクションで実行 |
| プッシュ通知送信（落札・承認通知） | Edge Function | Web Push APIへの外部HTTPリクエストが必要 |
| 写真提出時のバリデーション・処理 | Edge Function | Storageとの連携、外部処理を伴う |

DBトリガーまたは`pg_cron`からEdge Functionを呼び出すことで、両者を連携させる。

---

## 2. 「グループ完全分離」の二重防御（確定方針）

1. **RLS**：`wallets`・`secrets`・`auctions`等の全テーブルに、所属グループのメンバーのみアクセス可能なポリシーを設定
2. **PostgreSQL Function内の明示チェック**：RPC関数の引数として受け取った`group_id`が、呼び出しユーザーの所属グループと一致するかを関数冒頭で必ず検証
3. **pgTAPによるポリシーテスト**：「他グループのWalletを読み書きしようとするとRLSで拒否される」ことを自動テストで担保

```sql
-- pgTAPテストの例（イメージ）
BEGIN;
SELECT plan(1);

-- 別グループのWalletへのUPDATEが拒否されることを確認
SELECT throws_ok(
  $$ UPDATE wallets SET balance = balance + 100
     WHERE group_id = 'other-group-id' $$,
  'new row violates row-level security policy'
);

SELECT * FROM finish();
ROLLBACK;
```

---

## 3. PWA構成（ネイティブアプリなし・確定）

- ホーム画面への追加を促すUI（初回起動時のインストールバナー）を実装
- iOS Safari 16.4以降のWeb Push制約（ホーム画面追加済みでないと通知不可）はユーザーに明示し、オンボーディング時に「ホーム画面に追加」を案内するフローを設ける
- Service Workerでオフライン時の簡易キャッシュ（秘密の閲覧履歴など）に対応

---

## 4. MVPスコープ（ツール導入範囲・確定）

| フェーズ | 導入するツール |
| --- | --- |
| **MVP（Phase 1）** | React 19, TypeScript, Vite, TanStack Router/Query, Zustand, RHF, Zod, Tailwind, shadcn/ui, PWA基本構成, Supabase一式, Prisma（マイグレーション・型生成）, PostgreSQL Functions, RLS, seed.sql, Vitest, Testing Library |
| **Phase 2** | Playwright（E2E）, Oxlint, Oxfmt, pgTAPの網羅的整備, Web Push本格運用, Deno lint/fmt/check/testの厳格化 |

コア機能（グループ・秘密登録・ポイント・オークション）の実装を最優先し、周辺のLint/E2Eツールは機能が動いてから整備する。  
（ショップは廃止方針のためコア対象外。`docs/オークションルール.md` §6.1）

### 4.1 Phase 2に回す理由（最初から導入しない理由）

| ツール | 最初から導入しない理由 |  |
| --- | --- | --- |
| **Playwright（E2E）** | E2Eテストは画面のDOM構造・操作フローに強く依存する。MVP段階は画面構成・UIフローそのものが頻繁に変わるため、先にE2Eを書くと画面変更のたびにテストを書き直すことになり、「テストのメンテナンスコスト」が「機能実装の速度」を上回ってしまう。UIがある程度落ち着いてから書く方が投資対効果が高い | 最後の仕上げ段階 |
| **Oxlint / Oxfmt** | Rust製で高速という利点はあるが、Vite+と同様にエコシステムとして比較的新しく、ルールセットや設定オプションがまだ変化している段階。MVP開発と並行してlintルールの調整に時間を取られるリスクがあるため、まずはESLint/Prettier相当の最小構成（もしくはNode標準）で開発速度を優先し、コードベースが固まってから高速なOxlint/Oxfmtへ移行する方が手戻りが少ない | 最初から、初期設定で問題ない |
| **pgTATの網羅的整備** | pgTAPによるRLSテスト自体は重要だが、「網羅的に」整備するにはテーブル設計が確定している必要がある。MVP期はテーブル・カラム構成がまだ変わりうるため、先に網羅的なテストを書くとスキーマ変更のたびにテストの書き直しが発生する。最初は「グループ完全分離」など致命的な不変条件のテストのみに絞り、スキーマが安定してから対象を広げる | 重要な部分だけ実装 |
| **Web Push本格運用** | VAPIDキー管理、購読（subscription）のライフサイクル管理、iOSのホーム画面追加要件への案内UIなど、通知まわりは実装量・検証項目（実機での動作確認を含む）が多い。MVPで検証すべきは「オークション・ポイントのコアゲームループが成立するか」であり、通知は無くてもコア体験の検証はできるため、後回しにしてもリスクが小さい | 最後に時間が余るなら |
| **Deno lint/fmt/check/testの厳格化** | Edge Functions自体の設計（どの処理をFunctionに切り出すか）がまだ固まりきっていない段階で厳格なlint/型チェックを敷くと、設計変更のたびに引っかかって開発速度が落ちる。まずは緩めの設定で試行錯誤し、Edge Functionsの責務が固まってから厳格化する方が合理的 | 厳格化まではいらない。 |

**共通する考え方**：これらはいずれも「対象（UI・スキーマ・Edge Functionsの設計）が変わりやすい段階で先に固めると、変更のたびに手戻りコストが発生する」種類のツールである。逆に言えば、コア機能（要件書1〜6章）が動く状態になり、対象が安定してから導入した方が、同じ投資でより長く効果が持続する。小規模チームでは「完成させること」が最優先事項であるため、変更耐性の低い投資は後回しにする。

---

## 5. 未確定・要検討事項

- `pg_cron`によるオークション終了処理と、Edge Functionsへの通知トリガー連携の具体的な実装方式（issue #43で追跡）

**解決済み（2026-08-20整理）**:

- ~~Vite+への移行タイミング~~ / ~~Cloudflare Pagesのビルド設定（Vite出力との組み合わせ）~~ → 2026-08-17にFrontendをNext.js（App Router）へ変更したためVite自体を採用しなくなり、この2項目は無効化
- ~~PostgreSQL Functions・RLSポリシーなど、Prismaスキーマで表現できないSupabase固有SQLの管理方法~~ → `prisma/sql/<domain>/*.sql`に分離し`npm run db:sql`で適用する方式に確定・実装済み（`docs/アーキテクチャ.md` §3、`prisma/sql/README.md`）
- ~~ローカル開発DBを`prisma dev`にするかSupabase CLIのローカルスタックにするか~~ → 開発・本番ともSupabase本体のPostgresに接続する方式に確定。`prisma dev`（ローカルPostgres）はSupabaseに繋がない場合の代替として`npm run db:dev`に残す（`AGENTS.md`コマンド表）
- ~~本番DATABASE_URLをSupabaseのPostgres接続文字列に切り替えるタイミング~~ → 切り替え済み（開発時点から一貫してSupabase本体に接続している）