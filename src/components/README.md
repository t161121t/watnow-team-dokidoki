# components

特定の機能に属さず、アプリ全体で使う UI を置く。

- `ui/`: shadcn/ui を含む、業務知識を持たない汎用 UI プリミティブ
- 直下: PWA 更新案内や開発ツールなど、アプリ全体に組み込む共通 UI

一つの機能だけで使う UI は `src/features/<feature>/` に置く。複数機能で実際に共有する段階になってから、このディレクトリへ移す。

全体の配置基準は [`docs/ディレクトリ構成.md`](../../docs/ディレクトリ構成.md) を参照する。
