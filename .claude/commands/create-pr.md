---
description: コミット、プッシュ、PR作成を一括実行する。--wait で CI/レビュー待機も行う。
argument-hint: --issue <番号> --wait
---

`.agents/skills/create-pr/SKILL.md` の手順に従って PR 作成を実行してください。

引数:

- `--issue <番号>`: Issue 紐付け
- `--wait`: CI / レビュー待機と自動修正を有効化
